# Beispiel-Umgebungsvariablen für den Mail-Server
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your_smtp_username
SMTP_PASS=your_smtp_password

# Absender (optional, sonst wird SMTP_USER verwendet)
SMTP_FROM=no-reply@example.com

# Zieladresse für eingehende Kontaktformular-Nachrichten
TARGET_EMAIL=philipp@philyourvoice.at

# Port für Express Server
PORT=5000