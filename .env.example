# Beispiel-Umgebungsvariablen für den Mail-Server
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your_smtp_username
SMTP_PASS=your_smtp_password

# Absender (optional, sonst wird SMTP_USER verwendet)
SMTP_FROM=no-reply@example.com

# Zieladresse für eingehende Kontaktformular-Nachrichten
TARGET_EMAIL=philipp@philyourvoice.at

# Port für Express Server
PORT=5000

# Optional: Express dient auch die React-Build-Dateien aus
# Setze auf 'true', wenn Frontend und Backend aus demselben Node-Prozess laufen sollen
SERVE_STATIC=true

# Frontend: Basis-URL der API für Produktion, falls API auf anderem Host läuft
# In CRA müssen REACT_APP_* Variablen gesetzt werden, damit sie im Frontend verfügbar sind
# Beispiel: https://api.deine-domain.tld
REACT_APP_API_BASE=

# Liste erlaubter Origins (CORS) für die API, kommasepariert
# Beispiel: https://www.deine-domain.tld,https://deine-domain.tld
ALLOWED_ORIGINS=

# Optional: Betreff-Präfix und BCC für eingehende Mails
# Beispiel: EMAIL_SUBJECT_PREFIX=[Kontakt]
EMAIL_SUBJECT_PREFIX=
# Optional: eine BCC-Adresse für Archivierung (z.B. kontakt-archiv@...)
EMAIL_BCC=